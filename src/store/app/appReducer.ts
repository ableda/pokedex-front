import { AppState, initialState } from './appState';

export const loginReducer = (state: AppState = initialState, action: any): AppState => {
  switch (action.type) {
    default:
      return state;
  }
};
